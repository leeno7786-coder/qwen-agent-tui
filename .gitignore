# dependencies (bun install)
node_modules

# output
out
# dist is prebuilt and shipped so the package runs even if `prepare` is blocked
dist
!dist/
*.tgz
# Built release tarball (kept for convenience; remove if you prefer to gitignore)
!nanoagent-*.tgz

# code coverage
coverage
*.lcov

# logs
logs
_.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# caches
.eslintcache
.cache
*.tsbuildinfo

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store

# local caches and temp files
.qwen-graph/
fix_tools.py
test-output.txt
skills/test-*.json
